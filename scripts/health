#!/usr/bin/env bash
set -uo pipefail

if [[ -f .env ]]; then
  set -a
  source .env
  set +a
fi

function check() {
  local port="$1"
  local label="$2"
  echo "== ${label} :${port} =="
  if response=$(curl -sf --connect-timeout 3 --max-time 5 "http://127.0.0.1:${port}/v1/models" 2>&1); then
    echo "$response" | head -c 400
  else
    echo "(not responding)"
  fi
  echo ""
}

# Phase 6 full vLLM: All agents on vLLM AWQ
check "${THOR_PORT:-8001}" "Thor ⚡ Primary Commander (vLLM)"
check "${VALKYRIE_PORT:-8002}" "Valkyrie 🛡 Execution Specialist (vLLM)"
check "${ODIN_PORT:-8011}" "Odin 👁️ Supreme Architect (vLLM)"
check "${HEIMDALL_PORT:-8012}" "Heimdall 👁 Guardian (vLLM)"
check "${LOKI_PORT:-8013}" "Loki 🧠 Adversarial Intelligence (vLLM)"
check "${FRIGGA_PORT:-8014}" "Frigga 🌿 Knowledge Curator (vLLM)"

# Bifrost scheduler (hybrid mode)
BIFROST_PORT="${BIFROST_PORT:-8899}"
echo "== Bifrost Scheduler :${BIFROST_PORT} =="
if response=$(curl -sf --connect-timeout 3 --max-time 5 "http://127.0.0.1:${BIFROST_PORT}/v1/status" 2>&1); then
  echo "$response" | python3 -m json.tool 2>/dev/null || echo "$response" | head -c 600
else
  echo "(not responding)"
fi
echo ""
